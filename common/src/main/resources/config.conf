// === Player Data Storage ===
player-data {
  storage-type = "mysql"

  // === This section only works if storage-type is mysql or mariadb ===
  mysql {
    host = "localhost"
    port = 3306
    database = "minecraft"
    username = "user"
    password = "password"
    use-ssl = false
  }
}